target("core")
    set_kind("static")
    set_group("VKEngine")
    add_files("core/*.cpp")
    add_headerfiles("core/*.h")
    add_deps("renderer")
target_end()

target("renderer")
    set_kind("static")
    set_group("VKEngine")
    add_files("renderer/*.cpp")
    add_headerfiles("renderer/*.h")
    add_deps("common")
target_end()

target("common")
    set_group("common")
    set_kind("static")
    add_deps("deps")
    add_headerfiles("common/**.hpp", "common/**.h", {public = true})
    add_files("common/**.cpp")
target_end()

target("deps")
    set_group("common")
    set_kind("static")
    add_packages("glfw","stb", "directxtk","glm","vulkan-headers", {public = true})
target_end()
